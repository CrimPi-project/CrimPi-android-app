<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="end">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background_dark_navy"
        android:orientation="vertical">


        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/primary_navy"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:titleTextColor="@color/primary_text_color">


            <ImageButton
                android:id="@+id/bluetoothButton"
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:layout_gravity="start|center_vertical"
                android:layout_marginStart="7dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/bluetooth_scan"
                android:padding="4dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_bluetooth_white"
                app:tint="@color/primary_text_color"
                tools:ignore="TouchTargetSizeCheck,ImageContrastCheck" />


            <ImageButton
                android:id="@+id/menuButton"
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:layout_gravity="end|center_vertical"
                android:layout_marginEnd="7dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/navigation_drawer_open"
                android:padding="4dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_menu_white"
                app:tint="@color/primary_text_color"
                tools:ignore="TouchTargetSizeCheck" />

        </androidx.appcompat.widget.Toolbar>

        <View
            android:id="@+id/topLine"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@color/purple_200" />

        <!-- Fragment Container: This is where your fragments (Home, Freestyle, My Workouts) will be loaded -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragment_container"
            android:name="com.almogbb.crimpi.HomeFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            tools:layout="@layout/fragment_home" />

    </LinearLayout>

    <!-- Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@color/primary_navy"
        android:fitsSystemWindows="true"
        android:layoutDirection="ltr"
        app:headerLayout="@layout/nav_header_main"
        app:itemIconTint="@color/accent_blue_light"
        app:itemTextColor="@color/primary_text_color"
        app:menu="@menu/activity_main_drawer"
        app:itemBackground="@drawable/nav_item_background_selector"
        app:subheaderTextAppearance="@style/NavigationViewSectionHeader"
        style="@style/NavigationViewStyle"/>

</androidx.drawerlayout.widget.DrawerLayout>
